const { Schema, model } = require("mongoose");

const MedicineSchema = new Schema({
    title: {type: String, required:true},
    description: String,
    price: {type: Number, required: true}
})

export const Medicines = model('Medicines',MedicineSchema)

